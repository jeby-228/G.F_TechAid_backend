import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const NeedDetailPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>需求詳情</Title>
      <p>需求詳情頁面將在後續實作中完成</p>
    </div>
  );
};

export default NeedDetailPage;