import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const SheltersPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>避難所資訊</Title>
      <p>避難所資訊功能將在後續實作中完成</p>
    </div>
  );
};

export default SheltersPage;