import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const TaskDetailPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>任務詳情</Title>
      <p>任務詳情頁面將在後續實作中完成</p>
    </div>
  );
};

export default TaskDetailPage;