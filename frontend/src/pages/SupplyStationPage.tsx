import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const SupplyStationPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>物資站點詳情</Title>
      <p>物資站點詳情頁面將在後續實作中完成</p>
    </div>
  );
};

export default SupplyStationPage;